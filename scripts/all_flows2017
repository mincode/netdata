#!/bin/bash
#rwfilter --dport=5005 --pass=stdout ext2ext-S0_20170726.* | rwcut --fields=sIP,dIP,dPort,sTime,duration,etime

if [ $# = 0 ]; then
  echo "List all flows to all ports."
  echo "parameters: MM DD"
  exit
else
  mm=$1
  dd=$2
  year="2017"
  rwfilter --dport=0-65535 --pass=stdout /data/ext2ext/$year/$mm/$dd/* | rwcut --fields=sIP,sport,dIP,dport,sTime,etime

#ext2ext-S0_20170727.* | rwcut --fields=sIP,dIP,sTime,etime
fi
